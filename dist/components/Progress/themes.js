var _a, _b;
import { variants, scales } from "./types";
export var styleVariants = (_a = {},
    _a[variants.ROUND] = {
        borderRadius: "32px",
    },
    _a[variants.FLAT] = {
        borderRadius: 0,
    },
    _a);
export var styleScales = (_b = {},
    _b[scales.MD] = {
        height: "16px",
    },
    _b[scales.SM] = {
        height: "8px",
    },
    _b);
export default styleVariants;
